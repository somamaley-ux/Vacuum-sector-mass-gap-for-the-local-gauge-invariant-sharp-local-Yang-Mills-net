# Phase V notes

This phase rewrites the diagonalization layer so that **same-domain Gödel threat** now appears only
as the terminal member of an explicit prerequisite package.

Implemented upgrades:

1. Added **Theorem 5.1A** (`thm:godel-prerequisites`): the named prerequisite package for any
   same-domain Gödel threat.
   - faithful same-domain coding
   - governance-relevant readback
   - a standing-bearing code-predicate role
   - admissible same-domain fixed-point production
   - survival into quotient / trajectory structure

2. Added **Theorem 5.1B** (`thm:self-reference-prereqs`): exhaustion of same-domain self-reference
   prerequisites.
   - inert readback -> closure-inert
   - extensional gate renaming -> gate-collapsed negative fixed point
   - same-side refinement / independent authorizer -> impossible
   - no fifth self-reference route remains

3. Rebuilt **Theorem 5.2** (`thm:no-diagonalization`) as an explicit contradiction split.
   - standing-positive fixed point -> bivalence / unique-gate contradiction
   - standing-negative fixed point -> must regain force by repair, generation, carrier, or downstream
     restoration
   - all such restoration routes are now explicitly blocked in-paper

4. Added **Theorem 6.1A** (`thm:translation-filter`) promoting the former PA-style example into
   theorem form.
   - ordinary Gödel objections threaten closure only if translated into a same-domain distinction
   - no translation -> inert bookkeeping
   - translation -> must pass through the forbidden prerequisite package

5. Updated the abstract, section introduction, main-theorem proof, audit trail, and hostile-referee
   reading path so Phase V is visible in the proof spine.
